Créer un environnement virtuel puis installer les dépendances :

python -m venv .venv
source .venv/bin/activate   # Linux / macOS

pip install -r requirements.txt

Créer un dossier nommé :

original/

Y placer les documents PDF à envoyer.

Lancer ensuite le script :

python dossier_loc.py
